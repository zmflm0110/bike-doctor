# 따릉이 고장 예보 — 완성 계획

> 사람들이 귀찮아서 안 하는 고장 신고를, 앞사람들의 헛걸음 기록이 대신한다.

근거(초안 단계, `~/ideas/drafts/04-bike-doctor`): 서로 다른 두 사람이 연달아 빌리자마자 반납한 자전거는 다음 사람도 34% 가 헛걸음(평소 2.5%, 약 14배).
서울 1·3·6월 + 대전 타슈 5·10월 재현, 경보가 고장 신고보다 중앙값 20시간 먼저, 그 사이 평균 4.4명 헛걸음, 서울 하루 84~223명 예방 가능.

## 사용자에게 필요한 것 (먼저)

| # | 필요한 것 | 왜 | 언제까지 | 상태 |
|---|---|---|---|---|
| 1 | **서울 열린데이터광장 인증키** | 자전거별 대여이력 API(tbCycleRentData, **반납 즉시 올라옴**) + 실시간 대여소 현황 | Phase 3 | ✅ 2026-09-25 받음(키체인) |
| 2 | **공공데이터포털 인증키** — 전기차 충전소 정보(getChargerStatus) | 충전기 헛충전(Phase 6). 대여이력은 키 1 로 되므로 공공데이터포털 쪽은 필요 없어짐 | Phase 6 | ✅ 2026-09-25 받음(키체인), 5분마다 수집 중 |
| 3 | 목표 대회와 마감 (예: 한국코드페어, 청소년 AI 창의 경진대회, 과학전람회) | 일정·제출 형식(보고서/영상/시연)에 맞춰 Phase 7 을 짠다 | 이번 주 | 대기 |
| 4 | 디스크 확보 → Xcode 설치 (앱스토어) | 아이폰 네이티브 앱(선택). 웹앱(PWA)은 Xcode 없이 먼저 만든다 | Phase 4 전 | 굿노트 정리 중 |
| 5 | **현장 조사 2주** — 대여소 5곳+에서 자전거 150대+ 상태 기록(앱 '현장 조사' 탭, 절차 `docs/field_protocol.md`) | 사람이 본 고장 vs 엔진 판단 → **실측 정밀도**. 실시간 API 로 **그날 바로 대조** 가능 | Phase 5 | **지금 시작 가능** |
| 6 | 친구 5~10명 구조대 시범 | "3초 확인" 이 실제로 되는지 | Phase 5 | 대기 |
| 7 | (선택) 서울시설공단에 결과 공유·의견 요청 메일 — 보내는 건 사용자 결정 | 공단 피드백·협력 가능성, 실시간 내부 데이터 | Phase 6 | 선택 |
| 8 | 작품 이름 (제안: **헛걸음 제로**, 따릉 닥터, 고장 예보) | 앱·발표 | 아무 때나 | 대기 |
| 9 | 맥 충전기 연결·덮개 열어 두기 | 긴 분석·실시간 수집이 잠들면 멈춤 | 늘 | — |
| 10 | 아이폰에서 https 쓰기 — **공개 없이 집 와이파이 안에서만** 준비됨: `data/tls/ca.crt` 를 AirDrop → 프로파일 설치 → 인증서 신뢰 켜기(1분). 밖에서도 써야 하면 그때 공개 주소(Cloudflare Tunnel 등) 결정 | 아이폰 사파리는 https 에서만 위치·카메라(QR) 허용 | Phase 5 전 | **사용자 1분** |

키는 macOS 키체인에만 저장하고 코드·git 에는 넣지 않는다.

## Phase 계획 (각 Phase 는 합격 기준을 만족해야 끝)

### Phase 0 — 프로젝트 정리 (지금, 사용자 불필요)
- 초안 코드를 `~/bike-doctor` 로 옮겨 모듈화: `engine/`(경보 규칙), `analysis/`(검증), `data/`(내려받기 스크립트).
- 데이터 내려받기 자동화(서울 대여이력·고장신고·대여소, 대전 타슈).
- **합격**: 명령 하나로 5개월·2개 도시 핵심 표가 다시 나온다. 단위 테스트 통과.

### Phase 1 — 엔진 v1 확정 (사용자 불필요)
- 헛대여 정의(시간·거리 기준)와 경보 규칙(연속 횟수, 같은 사람 거르기, 선착장 기준 상향)을 **1월로 정하고 3·6월·대전으로 시험**(미리 본 데이터로 기준을 맞추지 않기).
- 민감도 표: 60/120/180초 × 100/200/300m, 연속 2/3회.
- 지표: 다음 사람 헛걸음 확률(정밀도), 신고 대비 선행 시간, 막은 헛걸음 수, 헛경보(경보 뒤 정상 이용) 비율.
- **합격**: 기준을 정한 달과 시험한 달의 결과 차이가 ±3%p 안, 표·그래프 완성.

### Phase 2 — 실시간으로 되나 (사용자 불필요, 키 없이 과거 기록으로)
- 실시간 공개 API 는 **대여소별 자전거 대수**만 준다(자전거 번호 없음).
- 과거 대여기록으로 대여소별 대수를 초 단위로 재구성 → 30초/60초/2분마다 찍은 것처럼 만든다 → "대수 −1 후 2분 안 +1" 깜빡임으로 헛대여를 잡는 **대여소 단위 경보**를 시험.
- **합격**: "공개 실시간 자료만으로 자전거 단위 경보의 X% 를, 정밀도 Y% 로" 를 숫자로 결론. X·Y 가 낮으면 대안(하루 늦은 자전거별 API, 구조대 제보) 확정.

### Phase 3 — 실시간 경보 서버 (키 1)  ← 2026-09-25 다시 바뀜
- 2026-09-25 발견: 서울 열린데이터광장 **대여이력 API(tbCycleRentData)가 자전거별 기록을 반납 즉시** 준다(지연 약 0분, 생년·성별 포함).
  Phase 2 의 "공개 실시간은 대여소 대수뿐" 은 실시간 대여소 API 만 본 결론이었다 → 이제 **서로 다른 두 번째 사람이 반납하는 순간 경보**.
- 같은 날(6/15)을 API 와 월별 파일로 받아 맞춰 봄 → 행·헛대여·아침 목록 모두 같음 → 파일로 잰 정밀도·선행 시간이 실시간에도 선다.
- `server/live.py`: 7일 채움 → 1분마다 갱신 → `web/data/live.json` + 경보 기록 → **실시간 경보 채점**(경보 뒤 다른 사람 첫 대여도 헛대여였나).
- **합격**: 7일 연속 실시간 운영, 실시간으로 알아챈 경보 채점 100건 이상, 정밀도가 파일의 **경보 한 건 기준 32~34%** 와 ±5%p 안
  (처음엔 '50~60% 안' 으로 적었는데 그건 경보 켜진 동안 모든 사람 기준이라 비교 대상이 틀렸음 — `docs/per_alarm.md`). 9/18~25 API 로 되짚은 값 30.7%.
- (아래는 그 전 계획 — 아침 목록은 실시간 창에서 그대로 만들 수 있어 남겨 둠)

#### (이전) 매일 아침 목록 서버 (키 2 필요, 키 1은 지도 표시용)  ← Phase 2 결과로 바뀜
- Phase 2 결론: 공개 실시간 대수만으로는 경보 정밀도 ≤1.7% → **자전거별 기록**으로 간다.
- 매일 새벽: 공공데이터포털 대여이력 API(키 2)로 어제 기록 수집 → 엔진 → **"신고 안 된 고장 의심 자전거" 아침 목록** → SQLite → REST API.
- 키 1(실시간 대여소 현황)은 지도에 자전거 대수 표시용.
- API 가 하루 단위로 갱신되지 않으면: 월별 파일로 시연 + "공단 내부 자료로 매일/실시간" 제안으로 전환.
- **합격**: 7일 연속 아침 목록 자동 생성, 목록 자전거의 실제 다음 날 헛걸음률을 매일 기록.

### Phase 4 — 앱 (웹앱 먼저, 이후 선택적으로 SwiftUI)
- **이용자**: 지도(대여소 + 아침 목록 자전거 표시), **QR·번호로 자전거 조회 → "어제 서로 다른 N명이 바로 반납했어요" 경고**.
- **구조대**: 가까운 경보 대여소 알림 → "체인/타이어/안장/멀쩡함" 3초 탭 + 사진(선택) → 서버 기록.
- **정비 기사**: 오늘 먼저 볼 대여소·자전거 순위표(지도 + 목록).
- **시연 모드**: 6월 기록을 빠르게 재생 — 경보가 켜지고, 헛걸음을 막고, 20시간 뒤 고장 신고가 들어오는 장면.
- **합격**: 사용자 아이폰 사파리에서 홈 화면 추가로 동작, 세 화면 + 시연 모드, 오프라인에서도 시연 모드 동작.

### Phase 5 — 현장 검증 (사용자 5·6)
- 2주 동안 집·학교 근처 경보 대여소 30곳 이상 확인 → 실측 정밀도.
- 친구 구조대 시범 → 확인 소요 시간, 참여율.
- **합격**: 실측 정밀도·표본 수·사진 기록이 보고서에 들어갈 형태로 정리.

### Phase 6 — 넓히기 (키 2 필요)
- 대전 타슈 실시간(가능하면), **전기차 충전기 헛충전**(같은 엔진) — "신고 대신 흔적" 을 두 번째 분야로 증명.
- (선택) 서울시설공단 제안서.
- **합격**: 충전기 1~2주 수집으로 헛충전 연쇄 검증 결과 한 장.

### Phase 7 — 대회 패키지
- **(마지막) 작품 소개 웹사이트** — 사용자 요청(2026-09-25): https://geulguard.jureuk.dev 같은 스타일(폰트·구성·기능까지 참고). 다른 것 다 끝난 뒤.
- 보고서(문제 → 데이터 → 방법 → 결과 → 한계 → 사회적 효과), 발표 슬라이드, 시연 영상(재생 시연 + 실제 앱 + 현장 확인 장면), 코드 정리·README.
- **합격**: 대회 제출 형식 체크리스트 전부 충족.

## 진행 기록
- 2026-09-24 Phase 0 시작. 프로젝트 폴더 생성, 데이터는 초안 폴더를 연결(data/raw).
- 2026-09-24 **Phase 0 완료**: `engine/core.py`(읽기·헛대여·연쇄·경보·지표), 단위 테스트 5개 통과, `data/download.py`(키 없이 원본 재수집),
  `analysis/report.py` 로 서울 3개월·대전 2개월 핵심 표를 100초에 재생성(`docs/results.md`). 연쇄2 → 다음 헛대여: 서울 34.8~37.9%, 대전 41.2~42.6%.
- 2026-09-24 **Phase 1 완료**: 1월 격자(60/120/180초 × 100/200/300m × 2/3연속)로 **3분·300m·서로 다른 사람 2연속** 선택(정점 240초와 0.3%p 차).
  같은 규칙을 그대로: 정밀도 서울 1월 54.1% → 3월 52.1%·6월 51.8%(차이 ≤2.3%p, 합격), 대전 58.5~60.2%. 3연속이면 약 70%.
  → 경보 두 단계(노랑 2연속·빨강 3연속). 그림 `docs/fig_chain.png`, 표 `docs/phase1.md`.
- 2026-09-24 **Phase 2 완료 — 방향 전환**: 공개 실시간 대수만으로는 대여소 경보 정밀도 0.2~1.7%(30초 간격이어도) → 불가.
  하루 늦은 자전거별 기록으로 아침 목록: 45~78대/일, 오늘 첫 이용자 헛걸음 34~42%, 66~82% 는 신고 없음 → Phase 3 을 "아침 목록 서버" 로 변경.
  (버그 기록: pandas 시각이 us 단위라 초 변환을 잘못해 처음엔 0% 가 나왔다 — 단위 가정 없이 고침)
- 2026-09-24 **Phase 4 v1**: 웹앱 `web/` (헛걸음 제로) — 아침 목록(지도·정비 순위·의심 자전거), 자전거 조회(번호·QR), 구조대 3초 확인(기기 저장),
  시연(6/15 하루 재생). 데이터 `analysis/export_web.py` → `web/data/`. 헤드리스 휴대폰 화면 검사 통과(오류 0).
  고친 버그: 재생 사건을 글자순 정렬해 "다음 날" 신고가 맨 앞에 와서 재생이 멈춤 → 초 단위 숫자로 정렬. 정비 순위를 대수 → 누적 헛걸음 순으로.
  남은 것: 아이폰 실기기 확인(QR 은 HTTPS 필요), 구조대 서버 전송(Phase 3), 시연에 지도 애니메이션.
- 2026-09-24 **Phase 3 뼈대**(키 없이 가능한 만큼): `server/seoul_api.py`(키체인 키, 없으면 sample 5건 — 실시간 현황·대여소 정보 동작 확인),
  `server/daily_job.py`(아침 목록 생성, 출처 file/api — api 는 명세 확인 전이라 안내 후 멈춤), `server/app.py`(웹앱 + 구조대 제보 SQLite, 표준 라이브러리),
  `engine/morning.py` 로 아침 목록 공용화(리팩터링 전후 결과 바이트 동일 확인). 테스트 6개 통과.
  확인한 사실: sample 키로 일별 이용정보(tbCycleRentUseDayInfo)는 이틀 전까지 올라와 있음(대여소 합계, 자전거 번호 없음).
  자전거별 대여이력 API 는 공공데이터포털 명세가 로그인 뒤에만 보여 **키 2 발급 뒤 확인**.
- 2026-09-24 **Phase 4 v2**: 시연 화면에 지도 애니메이션(경보 빨강·막을 수 있던 헛걸음 초록·뒤늦은 신고 청록), 구조대 제보 서버 전송 + 어느 탭에서든 보이는 알림.
  헤드리스 검사: 12시까지 재생 시 경보 99·막을 수 있던 헛걸음 156·뒤늦은 신고 17, 오류 0.
- 2026-09-24 **Phase 7 초안**: `docs/report.md` — 문제·아이디어·데이터·방법·결과(5개 표+그림)·실시간 한계·서비스·차이·한계·확장.
  핵심 결과표를 확정 규칙(3분·300m)으로 재생성(`docs/results.md`): 연쇄2 → 다음 포기 서울 35.3~37.1%, 대전 41.6~43.6%.
- 2026-09-24 **Phase 5 준비 완료**: 앱 '현장 조사' 탭(가까운 대여소/이름 검색, 번호 입력, 상태 6종, 폰 보관 후 재전송, 본 시각 저장, CSV),
  서버 `POST /api/survey`·`GET /api/survey.csv`, 번호 정리(norm_bike), `analysis/field_validation.py`(그달 기록 공개 뒤 포착률·헛경보율·적중률), 절차 문서.
  테스트 8개 통과, 헤드리스 검사(가까운 대여소 6m, 기록·CSV) 통과. 발견: 아이폰은 https 아니면 위치·카메라 차단 → 이름 검색·직접 입력으로 우회, https 는 사용자 결정 항목에 추가.
- 2026-09-24 정리: 엔진 기본 규칙을 확정값(3분·300m)으로, '연쇄 중 7일 무신고' 를 확정 규칙으로 재계산(서울 49~61%, 이전 56~69% 는 옛 규칙) → 보고서 수정.
  `README.md`(실행법), `docs/slides.md`(10장 발표 구성·예상 질문) 추가.
- 2026-09-24 고장 종류 짐작(`analysis/fault_kind.py`): 단말기 헛대여는 중앙값 13초(71% 30초 안), 타이어·체인·페달은 37~38초(14% 50m+ 이동) → 두 무리 구분 가능, 보고서 5-6.
- 2026-09-24 **Phase 6 뼈대**: `engine/ev.py`(스냅샷 → 끝난 충전 → 헛충전(3분)·재시도(10분 안)·연쇄·경보·'좀비 충전기'), `server/ev_collect.py`
  (환경부 getChargerStatus 수집 → data/ev.sqlite, --report), 테스트(가짜 스냅샷). 키 2 가 오면 서울(zcode 11) 5분 간격으로 1~2주 수집 → 검증.
  ※ 응답 필드·페이지 구조는 공개 설명 기준 — 첫 호출에서 확인 필요.
- 2026-09-24 **Phase 3 예행연습**(키 없이): 매일 아침 작업을 운영 코드 그대로(`run_morning`: 최근 7일 기록 → 오늘 목록 SQLite 기록 → 어제 목록 채점)
  6월 파일로 7일 연속 → 7일 모두 목록 생성·채점 쌓임, 한 달 전체 목록과 99.6% 일치, 목록 자전거 첫 이용자 헛걸음 42.3%(`docs/phase3_rehearsal.md`).
  창 1일이면 82% 일치 → 7일 창 확정. 키 2 가 오면 `fetch_rentals_api` 만 채우면 된다. 테스트 11개.
- 2026-09-24 **API 에 생년·성별이 없을 때 대비**(`analysis/no_who.py`, `docs/no_who.md`): 반납 뒤 N초 안 재대여를 같은 사람으로 — 1월로 120초 선택,
  3·6월 정밀도 51.9·51.8%(생년·성별 규칙 52.1·51.8%), 경보 수 거의 같음. 안 거르면 46.8·47.7%, 경보 +28%. 엔진에 `retry_gap_sec`, 아침 목록 기본값으로(서울 파일 결과는 바이트 동일).
- 2026-09-24 목록 설계 확인: 연쇄 뒤 2일+ 안 빌린 자전거(하루 약 200대)는 다음 사람 헛걸음 10~12% → 목록에서 빼는 현재 설계 유지(보고서 5-4).
- 2026-09-24 **집 와이파이 https**(`server/https_local.sh`, `app.py --https`): 작은 인증기관으로 맥 서버 인증서(이름 .local + 와이파이 IP) 하나만 서명하고
  인증기관 키는 바로 지움(아이폰에 믿게 해도 다른 사이트 인증서를 새로 만들 수 없음) + 이름 제약(.local·사설 IP만).
  확인: curl 로 이름·IP 둘 다 200, 인증기관 없으면 거부, 다른 이름이면 거부. 맥 Apple 검증기(security verify-cert, 아이폰과 같은 계열)도 통과·다른 이름 거부.
  아이폰 실기기 확인은 사용자(표 10번).
- 2026-09-24 **Phase 4 오프라인 합격 기준 확인·수정**: 서비스워커가 같은 출처만 캐시해서, 브라우저 HTTP 캐시가 비면(아이폰 홈 화면 앱에서 흔함) 오프라인에서
  지도 라이브러리(CDN)가 사라져 지도가 통째로 안 뜨는 것을 재현 → Leaflet·jsQR 를 `web/vendor/`(라이선스 포함)로, 본 지도 조각만 캐시(최대 500, 미리 받기 없음),
  대여소 전체를 옅은 점으로 깔아 조각이 없어도 서울 모양이 보이게. 검사(`tests/web/offline.js`): 서버를 끄고 지도 조각 요청까지 막은 진짜 오프라인
  3가지(보통·HTTP 캐시 비움·조각 캐시도 비움)에서 지도·QR 라이브러리·시연 재생 동작, 오류는 막은 조각 요청뿐.
  함정 기록: Playwright setOffline 은 서비스워커의 요청을 안 끊어서 처음엔 "고쳐졌다" 고 잘못 볼 뻔함 → 서버를 실제로 끄고 다시 잼.
- 2026-09-24 **보고서 5-6 주장 점검**(`analysis/fault_kind_per_bike.py`): "헛대여 모양으로 고장 종류 짐작" 은 평균 차이였을 뿐, 한 대씩 "단말기" 이름표를
  붙이면 맞는 비율 10~14%(AUROC 0.69~0.78, 단말기가 신고의 2~4% 로 드묾) → 보고서를 "한 대 판정은 못 함, 종류는 구조대 확인으로" 로 고침. 앱엔 원래 표시 안 함.
- 2026-09-24 **클라우드 세션**(원본 자료·서울/공공데이터 사이트 접속 불가 환경에서 가능한 것):
  웹앱 — QR·입력 글자를 innerHTML 에 그대로 넣던 것 이스케이프(QR 에 HTML 을 심는 장난 차단), 구조대 확인 결과(GET /api/rescue)를 의심 자전거에 표시하고 고장 확인된 대여소를 정비 순위 맨 위로.
  검사 — `tests/web/smoke.js`(다섯 탭 14항목, 임시 DB), `offline.js` 가 서버를 직접 띄우고 합격·불합격 종료 코드(예전 pkill 은 리눅스에서 자기 셸까지 죽였음), GitHub Actions.
  Phase 7 — 시연 영상 자동 녹화(`tests/web/record_demo.js` → `docs/demo/demo.mp4`, 1분 27초 자막), 발표 `docs/slides.html`(10장, 오프라인)·`slides.pdf`, 보고서 `report.pdf`(`docs/build_pdf.js`).
  Phase 3 — `fetch_rentals_api` 를 명세 없이 채움: 요청주소만 넣으면 하루씩·쪽 넘겨 받고(표준·odcloud 두 응답 형식, 한 건이면 딕셔너리로 오는 경우),
  열 이름은 `engine/core.py` FIELDS 로 맞춤(대여소번호 5자리 맞추기, 생년·성별 없으면 2분 규칙), 오류 코드는 크게 멈춤. 가짜 API 서버로 테스트 5개(총 16개).
  키는 환경변수로도(맥이 아닌 서버). 키 2 가 오면 할 일: 요청주소 확인 → `daily_job.py --source api --api-url ...`.
  아이폰 대비 — 홈 화면 아이콘이 SVG 뿐이라 아이폰은 무시하고 화면 캡처를 아이콘으로 씀 → PNG 180·192·512(`tools/make_icons.js`), 입력칸 16px 이상 검사.
  충전기 수집기 — 응답이 한 건이면 딕셔너리로 오는 경우, 오류 코드면 크게 멈춤.
  Phase 5 — 현장 조사 사진(폰에서 긴 변 1280px JPEG 로 줄여 보냄, 서버는 진짜 JPEG·PNG·크기 확인 뒤 `data/photos/`, CSV photo 열,
  사진이 거절돼도 기록은 살림, 폰 저장 공간이 차면 사진만 빼고 보관). 검증 스크립트: 조사한 자전거만 골라 계산(한 달 400만 건 → 수백 건),
  윌슨 95% 신뢰구간, 보고서용 `docs/field_validation.md`. 테스트 18개.
  Phase 6 (선택) — 서울시설공단 제안서 초안 `docs/proposal.md`·PDF(요청: 자전거별 기록 1시간 단위, 비용 없는 한 구 시범). 보내는 건 사용자 결정(표 7번).
  함정: 마크다운 변환기가 "12~23%" 의 물결표 한 쌍을 취소선으로 바꿈 → 물결표 이스케이프.
- 2026-09-24 앱 기능 셋(있는 자료만으로): 정비 동선(`web/route.js`, 탐욕법 + 2-opt, 위치를 켜면 내 근처 10곳), 구조대 가까운 순, 아침 목록 뒤돌아 채점
  (시연 날짜 6/15: 목록이 나온 뒤 첫 이용자 77명 중 29명 38% 또 반납). 대여소 이름 앞 빈칸(2,789곳 중 2,003곳) 정리. 동선 단위 검사 + 화면 검사 늘림.
- 2026-09-24 접근성: 탭 역할(tablist/tab/tabpanel), 입력칸 이름표, 알림을 화면 읽기 프로그램이 읽게(role=status), 지도 설명.
  글자 대비 검사(WCAG AA 4.5:1, 다섯 탭 × 밝은·어두운 화면)를 화면 검사에 넣었더니 어두운 화면의 청록 단추 흰 글자 1.9:1, 빨강 태그 2.3:1,
  밝은 화면 빨강 4.3·초록 4.1 이 걸림 → 어두운 화면은 어두운 글자, 빨강 #c2410c·초록 #237032 로.
- 2026-09-24 구(區) 고르기 + 정비 담당용 CSV(엑셀용 BOM, 서버 없이 폰에서). 함정: 한글 파일 이름은 헤드리스 크롬이 'download' 로 바꿈 → 영문 이름(morning_날짜_구.csv).
- 2026-09-24 자동 실행: `server/schedule.py`(launchd — 매일 06:10 아침 목록, 웹 서버 상시, 선택 충전기 수집; 키는 plist 에 안 넣고 키체인). 
  앱 기본 날짜를 '최근 3일 안 목록이면 가장 새 날, 아니면 6/15' 로(운영 시작하면 오늘 목록이 먼저 뜨게), `?day=`.
- 2026-09-24 **아이폰 앱(SwiftUI)**: "Xcode 설치" 요청 — Xcode 는 맥 전용이라 이 리눅스 세션엔 못 깖. 대신
  ① Swift 6.1.3(docker, 미러에서)으로 엔진 패키지 `ios/Core` 를 만들어 리눅스에서 검사 10개 — 웹앱에서 뽑은 정답과 순위·동선(1e-6m)·채점·구가 같음
  (자바스크립트 객체 키 순서까지 흉내내야 같아짐), ② 화면 `ios/App` 5개 탭(MapKit·QR 카메라·위치·사진·서버·폰 보관),
  ③ XcodeGen 을 리눅스에서 빌드해 `HeotgeoleumZero.xcodeproj` 생성, ④ GitHub 맥 러너(macos-15, Xcode 16)에서 **빌드 성공 + 시뮬레이터 실행 확인**, 탭별 화면 사진.
  함정: 정답 뽑는 스크립트가 날짜 바꾼 직후 옛 목록을 읽음 → 목록의 date 로 기다림. 커밋 메시지에 [skip ci] 를 쓰면 내 커밋도 CI 가 건너뜀.
- 남은 것(사람): 인증키 2개, 목표 대회, 현장 조사 2주, 아이폰 실기기 설치(맥 Xcode), 이름·학교, 제안서 보낼지.
- 2026-09-25 **키 두 개 받음 → 실시간으로 방향 전환**: 서울 대여이력 API(tbCycleRentData)가 반납 즉시 자전거별 기록을 줌(10:59:44 에 10:59:45 반납이 이미 있음 — 시계 차이뿐).
  6/15 하루를 API·월별 파일로 대조(`docs/api_parity.md`): API 150,830행·파일 150,827행 모두 짝, 헛대여 4,085/4,087, 다음 날 아침 목록 79대 동일.
  차이 둘을 고침 — 대여소는 5자리 번호(RENT_ID) 우선(ST-ID 아님), API 의 빈 성별('')·소문자 → 같은 사람 키 95.9% → 99.94% 일치.
  `server/live.py`(1분 갱신, 7일 창, 24시간 안 연쇄만, 경보 기록·실시간 채점), 웹앱 '지금 (실시간)'. 충전기 수집(5분) 시작. 테스트 21개.
- 2026-09-25 공간 확보(사용자 허락): 간직 프로젝트를 비공개 저장소 zmflm0110/ganjik 에 백업(코드·Zeroth 시험 데이터·결과·모델 목록, 실수로 커밋된 가상환경은 이력에서 제거)
  → 원격과 같음 확인 뒤 ~/ganjik 과 간직 전용 음성 모델 캐시(6.9GB) 삭제. 남은 공간 11GB → 22GB (Xcode 설치 가능).
- 2026-09-25 **정밀도 정의 점검**: 실시간 채점(경보 한 건마다 다음 다른 사람)을 9/18~25 API 로 되짚으니 30.7% — 파일의 "정밀도 52~60%" 와 달라 원인을 찾음.
  52~60% 는 경보가 켜진 동안 빌린 모든 사람 기준(긴 연쇄 가중), 같은 방식(경보 한 건 기준)으로 파일을 재면 32.1~34.3%(`analysis/per_alarm.py`) → 실시간과 일치.
  README·보고서·발표·제안서에 두 정의를 같이 적음. Phase 3 합격 기준도 이 숫자로 고침.
- 2026-09-25 충전기 30분 수집: 끝난 충전 2,509건, 헛충전(3분 안) 10.0%, 연쇄 안 끊긴 충전기 7대 — 1~2주 모아 검증.
- 2026-09-25 아이폰 앱 실시간 목록 지원(맥 CI 빌드), 운영 목록은 web/data/ops/(git 밖).
